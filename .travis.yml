language: python
python:
  - "2.7"
  - "3.6"
# Additional services(s) required
services:
  - redis-server
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - python -m unittest discover
